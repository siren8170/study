공통점
if, else, else if(elif), for, while, switch(match)등 기본적인 제어함수를 똑같이 다룬다
기본 제공되는 C의 자료형이 python에도 모두 있다.

차이점
print(i)
print('ab'+'cd)
x = 'hello'
print(x * 3)
와 같이 출력함수의 편의성이 뛰어나다

a[1:9]
a[4:]
a[:2]
a[::-1]
등 인덱스 지정 방식이 다양하다

sum, min, len, range 등 기본 내장 함수가 다양하다

변수 타입을 직접 지정하지 않아도 된다
예) x = 1, x = "oiqndiwqndoiw"

주석이 아닌 에러 없이 건너뛰게 해주는 pass가 있다.

for문에서 i는 리스트를 순회하며 값을 가져온다

리스트 이외에도 tuple(값을 못 바꾸는 리스트), dictionary(인덱스를 숫자 대신 고유 키로 사용하는 리스트), set(값의 중복이 없는 리스트) 등 다양한 리스트 유형이 존재하며 데이터를 다루는데 특화되어 있다.

구조체와 비슷하지만 더 큰 개념인 클래스(변수 뿐 만이 아닌 함수와 코드도 포함된 구조체)와 딕셔너리(구조체 변수)가 있다.
